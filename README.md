# 宝宝小程序 — 开发脚手架

此仓库为微信小程序（云开发）项目的起始骨架。当前由单人开发，目标在 3 周内完成 Full MVP（点歌、歌手响应、TodayPlaylist、knownSongs、ToLearn、导出/备份、权限与测试）。

已创建：
- `cloud/functions/`：云函数骨架目录
- `frontend/pages/`：前端页面占位目录

下一步（已开始 Day1）:
- 完成需求确认与配置
- 设计数据库 schema
- 实现 `recordRequest` 云函数骨架

运行与部署：
- 使用微信云开发控制台部署云函数与云数据库

如需我继续：我可以现在实现 `recordRequest` 的完整云函数及单元测试，或者先产出数据库 schema 文档。请回复 `recordRequest` 或 `schema`。